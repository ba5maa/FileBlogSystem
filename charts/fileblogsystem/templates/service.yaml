apiVersion: v1
kind: Service
metadata:
  name: {{ include "fileblogsystem.fullname" . }}
  labels:
    {{- include "fileblogsystem.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fileblogsystem.selectorLabels" . | nindent 4 }}
